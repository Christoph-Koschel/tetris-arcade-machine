export declare const PLUGINS: {
    TSB: {
        MINIFIER: "tsb.minifier",
        SHEBANG: "tsb.shebang",
        NODE: {
            LOADER: "tsb.node.loader"
        },
        PACKER: "tsb.packer"
    }
}
